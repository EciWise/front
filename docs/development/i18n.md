# Internacionalización

## Archivos

Las traducciones viven en:

```text
src/app/core/i18n/translations/
  es.ts
  en.ts
  fr.ts
  pt.ts
  de.ts
```

## Servicio

`I18nService` inicializa idioma y preferencia. `StaticTranslateLoader` carga objetos estáticos.

## Reglas

- Agregar la misma clave en todos los idiomas.
- Mantener la misma estructura de objetos.
- No hardcodear texto visible.
- Usar `TranslatePipe` en templates.
- Usar interpolación para valores dinámicos:

```html
{{ 'tutorias.seatsAvailable' | translate: { count: slot.availableSeats, total: slot.availability.capacity } }}
```

## Validación manual de claves

El proyecto no tiene todavía un script formal para comparar idiomas, pero se puede ejecutar una validación con Node tomando `en.ts` como base y comparando `missing` y `stale` en los demás idiomas.

## Nombres de dominio

Usar claves por dominio:

- `auth.*`
- `nav.*`
- `profile.*`
- `tutorias.*`
- `tutor.*`
- `admin.*`
- `chat.*`
- `common.*`

Evitar mezclar textos de un módulo en otro si no son compartidos.

